---
description: Genera y corre tests para la feature actual, según requirements.md/validation.md
agent: test-writer
---

**Contexto e instrucciones adicionales del usuario:** `$ARGUMENTS` (Toma esto como directiva principal).

Usa la skill `test-strategy`. Lee `requirements.md` y `validation.md` de la carpeta de la feature actual en `specs/`, escribe los tests que falten, córrelos, y reporta resultado (pass/fail y por qué).
